# Спецификация: ПланОбмена

## Назначение

Platform facts:

- `ВидЭлемента: ПланОбмена` описывает состав объектов, участвующих в обмене.
- В `Состав[]` указывается элемент конфигурации, включенный в план.

Local conventions:

- Имя задавай PascalCase по сценарию обмена.
- Минимальный пример держит локальный элемент рядом, чтобы ссылка состава разрешалась.

## Версия и источники

| Claim | Источник | Проверено | Что подтверждает |
| --- | --- | --- | --- |
| YAML-свойства плана обмена | `topics/latest/exchange-plan-properties` | 2026-07-28 | `ВидЭлемента`, `Состав`, поддерживаемые виды элементов |

## YAML

Required:

```yaml
ВидЭлемента: ПланОбмена
Ид: <UUID>
Имя: <Имя>
ОбластьВидимости: ВПодсистеме
Состав:
    -
        Элемент: <ИмяЭлемента>
```

Negative:

- Не оставляй `Состав[]` без `Элемент`.
- Не ссылайся на элемент, которого нет в проекте или комплекте примера.

## UUID

Required:

- UUID получает объект плана обмена.
- Строки `Состав[]` в подтвержденном минимальном контракте не получают UUID.

Negative:

- Не добавляй UUID строкам состава без source-backed требования.

## Imports и visibility

Required:

- `ОбластьВидимости` задавай явно.
- Если элемент состава находится в другой подсистеме, используй общий workflow `Импорт`/`Использование`/`ВПроекте`.

Negative:

- Не создавай план обмена с неразрешимой ссылкой состава.

## Companion artifacts

Required:

- `*.yaml` - описание объекта.

Negative:

- XBSL/XBQL companion не является обязательным для минимального плана обмена.

## Генерация

Required:

- Skill создает YAML плана и проверяет, что каждый `Состав[].Элемент` найден.
- Если элемент не найден, останавливайся или предлагай создать его отдельно.

Negative:

- Не создавай фиктивный объект состава молча в пользовательском проекте.

## Валидация

Required:

- `Состав` непустой список.
- Каждый `Элемент` разрешается внутри проекта.

Negative:

- `unresolved_composition`.

## Runtime evidence

Required:

- `object-coverage.json` фиксирует passed runtime check: 1cmycloud `test-app`, technology build `9.2.9-12`, assembly `TestApp 1.0-216.xasm`, image `019fa958-8ab0-70be-a647-735da5c7ec90`, update task `019fa958-8df1-7fbe-b1fc-a7191da92c5` completed on `2026-07-28`.

Negative:

- Future runtime status `passed` требует completed `UpdateApplicationConfiguration`; локальный lint/build без deploy не считается runtime evidence.

## Platform facts и local conventions

- Platform facts: `topics/latest/exchange-plan-properties` подтверждает YAML-схему и состав.
- Local conventions: локальная проверка разрешимости ссылок и layout минимального примера принадлежат `xbsl-meta-add`.
